# Cursor Prompt — Phase 00: Repository Audit & Project Setup

Read `CLAUDE.md` and all relevant files under `docs/`.

Do not implement wedding features yet.

Tasks:
1. Inspect the repository.
2. Identify current framework, dependencies, and existing architecture.
3. Compare current project against `docs/04-architecture/`.
4. Identify conflicts or risky assumptions.
5. Establish the documented folder structure only where necessary.
6. Preserve existing useful code.
7. Do not delete files unless explicitly justified.

Output before coding:
- current architecture
- conflicts
- implementation plan

After changes:
- run typecheck if available
- run lint if available
- run build if available

Update `docs/00-project/PROJECT_STATUS.md`.
